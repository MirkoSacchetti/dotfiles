dirname=`dirname $0`
ln "$dirname"/tmux.conf ~/.tmux.conf --force
ln "$dirname"/prettierrc ~/.prettierrc --force
ln "$dirname"/zshrc ~/.zshrc --force
ln "$dirname"/vimrc ~/.vimrc --force
ln "$dirname"/init.vim ~/.config/nvim/init.vim --force
